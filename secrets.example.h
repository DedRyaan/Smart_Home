#pragma once
// Copy this file to "secrets.h" (which is gitignored) and set your own values.
// config.h auto-includes secrets.h if present and uses these instead of defaults.

#define AP_PASSWORD  "your-setup-ap-password"   // >= 8 chars (WPA2 setup AP)
#define OTA_PASSWORD "your-ota-password"        // required to push OTA firmware
